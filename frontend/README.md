# EcoMon (Vite + React)

## Avvio sviluppo

```bash
npm install
npm run dev
```

Il backend locale va avviato separatamente dalla cartella `backend` con `python app.py`.

## Build produzione

```bash
npm run build
npm run preview
```
